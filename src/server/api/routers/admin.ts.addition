  getClubMembers: adminProcedure
    .query(async () => {
      const members = await db.clubMember.findMany({
        orderBy: {
          lastName: 'asc',
        },
      });
      return members;
    }),
